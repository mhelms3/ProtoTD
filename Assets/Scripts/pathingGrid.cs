using UnityEngine;
using System.Collections;

public class pathingGrid {

    private node[,] nodes;

    public pathingGrid (int _maxX, int _maxY)
    {
        nodes = new node[_maxX, _maxY];
    }


	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
